fastapi==0.121.2
uvicorn[standard]==0.38.0
sqlalchemy==2.0.44
pydantic==2.12.4
pydantic-settings==2.13.0
# ⚠️ **Mindestens 0.0.30.** 0.0.20 laesst sich mit vielen oder sehr langen
# Multipart-Kopfzeilen lahmlegen (GHSA-pp6c-gr5w-3c5g, behoben in 0.0.27), und
# eine Anfrage mit `;` als Trenner kostet quadratisch Rechenzeit
# (GHSA-5rvq-cxj2-64vf, behoben in 0.0.30). Beides ist hier erreichbar: Vor der
# Ersteinrichtung nimmt `/api/sicherung/einspielen-vor-einrichtung` einen
# Upload **ohne Anmeldung** entgegen.
#
# Die dritte Meldung (GHSA-wp53-j4wj-2cfg, Pfad-Ausbruch) greift hier nicht:
# Sie verlangt UPLOAD_DIR zusammen mit UPLOAD_KEEP_FILENAME=True, und beides
# setzt FastAPI nicht.
python-multipart==0.0.32

# Passwoerter. Argon2id statt bcrypt: nexmail haelt alle Postfaecher seines
# Betreibers, und speicherharte Ableitung ist den Unterschied wert.
argon2-cffi==25.1.0

# Zweiter Faktor. pyotp fuer TOTP, segno fuer den QR-Code - beides klein und
# ohne eigenes Netz.
pyotp==2.9.0
segno==1.6.6

# Verschluesselung der Zugangsdaten (AES-GCM).
# ⚠️ **Mindestens 50.0.1.** Die Wheels bringen ein statisch gebundenes OpenSSL
# mit, und alles davor traegt Meldungen dagegen (GHSA-537c-gmf6-5ccf). Die
# naheliegende Behebungsversion 48.0.1 reicht **nicht**: Am 01.09.2026 gegen
# api.osv.dev geprueft, stehen fuer 48.0.1 noch sechs weitere Meldungen, fuer
# 50.0.1 keine.
cryptography==50.0.1

# Verschluesselte Sicherungs-Archive (AES-ZIP). Bewusst ein Standardformat:
# ein selbstgebautes koennte nur nexmail wieder oeffnen - und ausgerechnet
# dann, wenn man die Sicherung braucht, laeuft nexmail vielleicht nicht.
pyzipper==0.4.0

# IMAP. Synchron und mit IDLE - fuer eine Handvoll Postfaecher ist asyncio
# kein Gewinn und viel mehr Fehlersuchaufwand.
#
# ⚠️ **Mindestens 3.1.0.** Version 3.0.1 laesst sich unter Python 3.14 nicht
# einmal verbinden: imaplib hat `file` zu einer Eigenschaft ohne Setter
# gemacht, und IMAPClient weist dort noch zu - AttributeError, bevor
# ueberhaupt eine Anmeldung stattfindet. Der Container faehrt zwar 3.13, aber
# die Entwicklungsumgebung 3.14, und der Fehler sah dort aus wie ein Problem
# des Mailservers. Siehe tests/test_umgebung.py.
IMAPClient==3.1.0

# Fuer die Autoconfig-Abfrage (Thunderbirds ISPDB und autoconfig.<domain>).
httpx==0.28.1

# HTML entschaerfen - beim Anzeigen fremder Mails UND beim Senden eigener.
# ⚠️ **Fehlte hier bis zum 01.09.2026**, obwohl der Code es seit Stufe 2
# benutzt: In der Entwicklungsumgebung war es von Hand installiert, im Abbild
# nicht. Der Container startete deshalb gar nicht - "ModuleNotFoundError: No
# module named 'nh3'", und zwar erst beim ersten `docker compose up`, Wochen
# nach dem Einbau.
nh3==0.3.7

# Zeichensatz erraten, wenn eine Mail luegt oder schweigt.
charset-normalizer==3.5.1

# OIDC-Anmeldung: die Unterschrift des ID-Ausweises pruefen.
#
# ⚠️ **Festgenagelt.** nexview hat gegen genau diese Fassung geprueft, dass
# ES512 und EdDSA ueber PyJWK tragen — beides laesst Pocket ID einstellen,
# und was hier fehlt, sperrt bei diesem Anbieter **jede** Anmeldung aus.
# ⚠️ **Mindestens 2.13.0.** 2.9.0 bis 2.12.1 pruefen beim Entschluesseln mit
# einem ``PyJWK``-Schluessel zwar den ``alg``-Kopf gegen die Verfahrensliste,
# ueberpruefen die Unterschrift danach aber mit dem Verfahren des Schluessels
# (GHSA-jq35-7prp-9v3f, CVSS 5.4). nexmail ist betroffen, weil es
# ``jwt.PyJWKClient`` benutzt (services/oidc.py) — anders als Nexview, wo
# dieselbe Meldung ins Leere geht.
#
# ⚠️ **Der Verwechslungs-Angriff ist davon NICHT betroffen.** Die schwerere
# Meldung GHSA-xgmm-8j9v-c9wx verlangt ausdruecklich ein symmetrisches
# Verfahren in ``algorithms=[...]``; VERFAHREN in services/oidc.py enthaelt
# keines. Wer diese Zeile spaeter liest, soll nicht den falschen Schluss
# ziehen: Der Schutz stand und steht.
pyjwt==2.13.0

# Die Zeitzonendatenbank als Paket.
#
# ⚠️ **Ohne sie kennt Python auf Windows KEINE einzige Zeitzone.** Am
# 02.09.2026 nachgemessen: ``available_timezones()`` gab dort **null** zurueck,
# und jede ``ZoneInfo("Europe/Berlin")`` scheiterte. nexmail faellt an solchen
# Stellen still auf UTC zurueck — die Uhrzeit im Ausdruck, das Aufraeumdatum,
# der Zeitraum der Abwesenheitsnotiz und die Zeit einer Termin-Einladung waren
# in der Entwicklungsumgebung damit um Stunden verschoben, im Container aber
# richtig. Genau die Sorte Unterschied, die man erst beim Nutzer findet.
#
# Im Abbild liegt die Datenbank ohnehin im System; das Paket kostet dort
# nichts und macht beide Umgebungen gleich.
tzdata==2026.1
